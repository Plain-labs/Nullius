# Dependencies
node_modules/

# Build outputs
dist/
target/
build/

# Rust incremental build cache
**/.fingerprint/
**/incremental/

# Circuit artifacts (generated — not source)
circuits/build/
circuits/keys/*.ptau
circuits/keys/*.zkey
circuits/keys/verification_key.json

# Contract deploy artifacts
.contract_addresses.json
sdk/src/contract_ids.json

# Environment — ignore real secrets, but track the example template
.env
.env.local
.env.*.local

# Lock files — track Cargo.lock (app, not lib), skip bun.lock (generated)
bun.lock

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
